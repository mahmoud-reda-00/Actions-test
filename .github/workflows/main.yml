name: first workflow

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: print welcome message 
        run: echo "welcome to first woekflow"

      - name: checkout repo
        uses: actions/checkout@v6.1.0
        
      - name: list repo files 
        run: ls -la
  
  
