on:
  workflow_dispatch:

jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
    - run: | 
        npm install
        npm run buld
      working-directory: ./tmp
      shell: bash
    -
    
      
      
